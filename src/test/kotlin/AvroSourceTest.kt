package io.toolisticon.avro.kotlin

import org.junit.jupiter.api.Test

internal class AvroSourceTest {

  @Test
  fun `create from schema json`() {
//    val source = AvroSource(JsonString(Schema.create(Schema.Type.STRING)))
//
//    Assertions.assertThat(source).isInstanceOf(JsonSource::class.java)
  }
}
